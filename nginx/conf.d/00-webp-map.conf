# Sidecar suffix for clients that advertise WebP, empty for everyone else.
# Loaded at http level so every server block can use $webp_suffix.
map $http_accept $webp_suffix {
    default   "";
    "~*webp"  ".webp";
}
