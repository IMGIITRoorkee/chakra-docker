# Serve a .webp sidecar for page assets when one exists, otherwise the original.
# A prefix location, so longer prefixes such as /~user and short-url routes
# keep their own alias. Include only where assets are served from disk.
location ^~ /assets/ {
    add_header Vary Accept;
    try_files $uri$webp_suffix $uri =404;
}
