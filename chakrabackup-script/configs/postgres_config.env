BACKUP_DIR=${BASE_DIR}/postgres-backup
CHAKRA_DIR=/home/root1/chakra-docker/
DAYS_TO_KEEP=14
FILE_SUFFIX=backup.sql


DIR1=`date +"%Y-%m-%d-%H-%M"`
FILE2=`date +"%Y-%m-%d-%H-%M"`${FILE_SUFFIX}

OUTPUT_FILE=${BACKUP_DIR}/${FILE2}
OUTPUT_FILE_VERBOSE=${BACKUP_DIR}/"verbose"${FILE2}

